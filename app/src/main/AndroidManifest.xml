<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.purnota.hoorayzone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/hooray_zone_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".WatchlistActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".DeveloperActivity" />
        <activity android:name=".AvailableTripsActivity" />
        <activity android:name=".SpecialActivity" />
        <activity android:name=".PreviousTripsActivity" />
        <activity android:name=".SajekActivity" />
        <activity android:name=".CoxActivity" />
        <activity android:name=".SundarbanActivity" />
        <activity android:name=".FeedBackActivity">

        </activity>
    </application>

</manifest>